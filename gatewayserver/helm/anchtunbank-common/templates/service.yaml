apiVersion: v1
kind: Service
metadata:
  name: {{ include "anchtunbank-common.fullname" . }}
  labels:
    {{- include "anchtunbank-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anchtunbank-common.selectorLabels" . | nindent 4 }}
