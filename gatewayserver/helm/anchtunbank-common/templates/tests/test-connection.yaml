apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "anchtunbank-common.fullname" . }}-test-connection"
  labels:
    {{- include "anchtunbank-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "anchtunbank-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
